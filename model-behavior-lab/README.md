# Model Behavior Lab

Promise: produce reproducible, evidence-linked model capability profiles.

AI Strength Comparator owns experiment/run/report behavior. Ethics scenarios, chess, structured
output, and collaborator-owned tic-tac-toe become benchmark packs or adapters. The durable product
is the corpus and reproducibility—not a model dropdown or an extra dashboard.

First move: prove one ethics scenario through the shared `ExperimentRun`, including benchmark and
scorer versions, raw evidence, retries, partial-run semantics, and cost.

Verified: none (0/5). All 5 waves carry source-backed prototype receipts; a prototype
proves only what its runner read, never recovered functionality.

Next wave: M1 — map one ethics scenario and result into `ExperimentRun`, matching the original
on prompt, response, score, metadata, and failures.
