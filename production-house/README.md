# Production House Suite

Promise: move creative work through resumable jobs to verified deliverables.

Production Engine owns entity, task, event, plugin, audit, and recovery semantics. Groundwire is the
first demanding domain pack. ElevenLabs Screenplay Formatter is the synthesis/export adapter;
Writers Room is collaborative story state. Quiznauts, Rockit, and face blur join only where they
actually reuse the job/state/evidence contracts.

The suite-local engine now enforces legal job transitions, immutable terminal states, unique named
artifacts, lowercase SHA-256 identities for every input and output, and a local episode-script
parser. P1–P5 inspect real Groundwire scripts, formatter plays, and Writers Room finals; receipts
still say that no formatter, voice provider, media pipeline, external Writers Room runtime, or
human signoff was invoked. Outputs remain modeled.

Verified: P1, P2, P3, P4, P5 (5/5) source-inspected analysis milestones. The next runtime move is
still to run one real Groundwire episode slice through the formatter and compare its artifacts,
failures, and QC behavior against `ProductionJob`.
